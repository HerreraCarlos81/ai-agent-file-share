# Provided by your File Share backend operator (Function URLs or API Gateway)
FILELINK_REGISTER_URL=https://your-register-endpoint.example/
FILELINK_UPLOAD_URL=https://your-upload-endpoint.example/

# Filled by the agent on first registration — never commit real values
FILELINK_EMAIL=
FILELINK_TOKEN=
